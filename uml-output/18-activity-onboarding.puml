@startuml
title 适老化打车平台 - 首次引导与设置流程活动图 (Onboarding & Settings Activity Diagram)

|老年用户|
start
:首次打开APP;

:显示欢迎页\n"欢迎使用适老化打车\n让出行更简单";

|老年用户|
if (是否跳过引导?) then (跳过)
  :直接进入首页\n(使用默认设置);
  note right: 默认字体: 1.0x\n默认对比度: 标准\n可在设置中修改
  stop
else (开始设置)
endif

== Step 1/3: 字体大小设置 ==
|系统|
:显示字体设置页\n"1/3 调整字体大小";

|系统|
:渲染字体预览区\n"这是标题文字\n这是正文文字，用于预览";

|老年用户|
:拖动字体滑块\n(1.0x - 2.0x);

|系统|
:实时更新预览文字大小;

|系统|
if (字体 >= 1.8x?) then (是)
  :显示提示\n"大字模式已开启\n部分内容可能需要滚动查看";
  note right: 极端字体缩放提醒\n避免布局溢出
else (否)
endif

|老年用户|
:点击"下一步";

== Step 2/3: 对比度与动画 ==
|系统|
:显示辅助设置页\n"2/3 辅助功能设置";

|系统|
:显示开关选项\n☐ 高对比度模式\n☐ 减少动画效果;

|老年用户|
if (是否开启高对比度?) then (开启)
  |系统|
  :切换为高对比度主题\n(对比度 ≥ 7:1);
  :实时预览对比度效果;
  note right: WCAG 2.2 AAA标准\n正常文本 7:1
else (不开启)
endif

|老年用户|
if (是否减少动画?) then (开启)
  |系统|
  :禁用过渡动画\n(instant transition);
  :禁用脉冲/闪烁效果;
  note right: 减少眩光\n降低认知负荷
else (不开启)
endif

|老年用户|
:点击"下一步";

== Step 3/3: 家庭地址 ==
|系统|
:显示地址设置页\n"3/3 设置家庭地址";

|系统|
:显示地址输入选项\n[手动输入] [语音输入] [暂不设置];

|老年用户|
if (选择输入方式?) then (手动输入)
  |老年用户|
  :在输入框中输入地址;
  |系统|
  :地址自动补全\n(POI搜索);
  |老年用户|
  :从候选列表中选择;

else (语音输入)
  |老年用户|
  :点击麦克风;
  :说出家庭地址;
  |系统|
  :语音识别地址;
  :展示识别结果;
  |老年用户|
  :确认或重新说;

else (暂不设置)
  :跳过地址设置;
  note right: 可在设置中\n随时补充
endif

|系统|
if (地址已设置?) then (是)
  :显示确认信息\n"家庭地址: 幸福小区3号楼";
  :保存到AsyncStorage;
  note right: 持久化存储\n应用重启后保留
else (否)
  :首页显示提示\n"请先设置家庭地址";
endif

== 完成引导 ==
|老年用户|
:点击"完成设置";

|系统|
:保存所有设置\n{fontScale, highContrast, reducedMotion, homeAddress};

|系统|
:持久化到AsyncStorage\n+ 同步到后端(如已登录);

|系统|
:跳转首页\n应用已保存设置;

|系统|
:显示欢迎提示\n"已为您设置好\n现在可以一键打车了";

stop

== 后续设置修改 ==
partition "设置页面" {
  |老年用户|
  :打开设置页\n(底部导航或首页);

  |系统|
  :加载当前设置\n从AsyncStorage读取;

  |系统|
  :渲染设置页面\n- 字体大小滑块\n- 高对比度开关\n- 减少动画开关\n- 家庭地址编辑\n- 紧急联系人管理\n- 历史行程查看\n- 使用帮助;

  |老年用户|
  repeat
    :修改某项设置;
    |系统|
    :实时预览效果;
    |系统|
    :保存到AsyncStorage;
  repeat while (继续修改?)

  |老年用户|
  :返回首页;

  |系统|
  :应用更新后的设置;
}

stop

@enduml