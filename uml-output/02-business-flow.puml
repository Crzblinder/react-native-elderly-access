@startuml
|老年用户|
title 适老化打车平台 - 业务流程图 (Activity Diagram)
start
:打开APP;
:首页展示\n(大字模式);

if (选择叫车方式?) then (一键打车回家)
  :点击"一键打车回家";
  if (已设置家庭地址?) then (是)
  else (否)
    :提示设置地址;
    stop
  endif
else (语音叫车)
  :点击麦克风;
  :语音输入目的地;
  :语音识别与意图解析;
endif

:大字弹窗确认目的地;

if (确认?) then (确认叫车)
else (重新输入)
  :返回输入;
  detach
endif

|系统|
:匹配附近司机;
:推送订单给司机;

|司机|
:司机接单;

|系统|
:更新订单状态;

|老年用户|
:等待接驾\n(语音播报状态);
:查看司机信息\n(车牌号、预计到达);

|司机|
:到达上车点;

|老年用户|
:上车并开始行程;

|系统|
:行程结束，计算费用;

|老年用户|
:选择支付方式;

if (支付方式?) then (自己支付)
  :一键支付;
else (亲友代付)
  :选择联系人;
  :发送SMS支付链接;

  |亲友用户|
  :打开支付链接;
  :确认代付;

  |系统|
  :更新支付状态;
endif

|老年用户|
:查看支付结果;

stop
@enduml
